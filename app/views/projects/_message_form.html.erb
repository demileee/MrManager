<%= form_for @message, url: project_messages_path(@project.id), method: :post do |f| %>
  <div class="field">
    <%= f.text_area :message_body, cols: "60", rows: "8" %>
  </div>
  <div class="submit">
    <%= f.submit "Add Note"%>
  </div>
<% end %>
