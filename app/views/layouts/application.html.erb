<!DOCTYPE html>
<html>
  <head>
    <title>MrManager</title>
    <%= csrf_meta_tags %>

    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script type="text/javascript" src="https://addevent.com/libs/atc/1.6.1/atc.min.js" async defer></script>
  </head>

  <body data-hex="<%= new_image.hex %>" style="background-image:url(<%= new_image.image %>)";>
    <% if session[:user_id] %>
    <header>
      <div class="logged-in-left">
        <%= link_to "Projects", projects_path(current_user) %>
      </div>
        <div class="logged-in">
          <%= link_to "#{current_user.first_name}'s Dashboard", user_path(current_user) %>
          | <%= link_to "Logout", :logout, method: :delete %>
        </div>
    </header>
    <% end %>

    <main>
      <%= yield %>
    </main>

  </body>
  <footer>
    <p>Image Courtesy of <%= link_to "Unsplash", "http://unsplash.com", target: "_blank" %>. Photographer: <%= new_image.photographer %> (see their <%= link_to "portfolio", new_image.portfolio_link, target: "_blank" %>).</p> 
  </footer>
</html>
